name: Build Alisa IPA

on:
  workflow_dispatch:
    inputs:
      configuration:
        description: '构建配置'
        required: true
        default: 'Debug'
        type: choice
        options:
          - Debug
          - Release
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app
          xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Download SPM dependencies as local sources
        run: |
          set -x
          cd $GITHUB_WORKSPACE
          mkdir -p .deps

          rm -rf .deps/GRDB
          git clone --depth 1 https://github.com/groue/GRDB.swift.git .deps/GRDB

          rm -rf .deps/ZIPFoundation
          git clone --depth 1 https://github.com/weichsel/ZIPFoundation.git .deps/ZIPFoundation

          rm -rf .deps/GCDWebServer
          git clone --depth 1 https://github.com/swisspol/GCDWebServer.git .deps/GCDWebServer

          rm -rf .deps/Highlightr
          git clone --depth 1 https://github.com/raspu/Highlightr.git .deps/Highlightr

          # Debug: list source directories
          echo "=== Source directories ==="
          for dir in .deps/*/; do
            echo "--- $dir ---"
            find "$dir" -name "*.swift" -o -name "*.m" -o -name "*.h" -o -name "*.modulemap" 2>/dev/null | head -10
          done

          rm -rf .deps/GRDB/.git .deps/ZIPFoundation/.git .deps/GCDWebServer/.git .deps/Highlightr/.git 2>/dev/null || true

      - name: Generate Xcode Project
        run: |
          cd $GITHUB_WORKSPACE
          rm -rf Alisa.xcodeproj
          xcodegen generate

      - name: Build (Archive)
        run: |
          cd $GITHUB_WORKSPACE
          xcodebuild archive \
            -project Alisa.xcodeproj \
            -scheme Alisa \
            -archivePath build/Alisa.xcarchive \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Create IPA
        run: |
          cd $GITHUB_WORKSPACE
          mkdir -p build/Payload
          cp -r build/Alisa.xcarchive/Products/Applications/Alisa.app build/Payload/
          cd build && zip -r Alisa-unsigned.ipa Payload
          rm -rf Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Alisa-Debug
          path: build/Alisa-unsigned.ipa
          compression-level: 0
          if-no-files-found: error